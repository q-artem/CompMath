#set page(
  paper: "a4",
  margin: (top: 2cm, bottom: 2cm, left: 3cm, right: 1.5cm),
)

#set text(
  size: 14pt,
  lang: "ru",
  font: "Times New Roman",
)

#set par(justify: true)

#show heading.where(level: 4): set text(weight: "regular", size: 1em)
#show heading.where(level: 4): set block(sticky: false)
#set heading(supplement: none)

#show link: set text(fill: blue)
#show link: underline

// --- 1. Титульный лист ---
#align(center)[
  #set text(size: 11pt)
  Министерство науки и высшего образования Российской Федерации \
  Федеральное государственное автономное образовательное учреждение высшего образования \
  #strong[«Национальный исследовательский университет ИТМО»] \
  #v(0.5em)
  Факультет Программной Инженерии и Компьютерной Техники
]

#v(1fr)

#align(center)[
  #set text(size: 16pt)
  #strong[Лабораторная работа №3] \
  #v(0.5em)
  #set text(size: 14pt)
  по дисциплине «Вычислительная математика» \
  Тема: «Численное интегрирование»
]

#v(1em)

#align(center)[
  Вариант: #strong[6]
]

#v(1.2fr)

#align(right)[
  #block(width: 50%)[
    #align(right)[
      #strong[Преподаватель:] \
      Бострикова Дарья Константиновна \
      #v(1em)
      #strong[Выполнил:] \
      Пшеничников Артём Дмитриевич \
      #strong[Группа:] Р3207
    ]
  ]
]

#v(1fr)

#align(center + bottom)[
  Санкт-Петербург, 2026
]

#pagebreak()

#outline(depth: 3)

#set page(numbering: "1")

#pagebreak()

= Цель работы
Найти приближенное значение определенного интеграла с требуемой точностью различными численными методами.

= Порядок выполнения работы
1. Изучение теоретических основ численного интегрирования
2. Программная реализация методов прямоугольников (левых, правых, средних), трапеций и Симпсона
3. Реализация алгоритма автоматического выбора шага разбиения на основе правила Рунге
4. Проведение численных экспериментов для заданного набора функций
5. Выполнение расчетного задания
6. Анализ полученных результатов и оценка погрешностей

= Рабочие формулы методов

== Методы прямоугольников
Шаг интегрирования: $h = (b - a) / n$.
- *Левые прямоугольники:* $I approx h sum_(i=0)^(n-1) f(x_i)$
- *Правые прямоугольники:* $I approx h sum_(i=1)^(n) f(x_i)$
- *Средние прямоугольники:* $I approx h sum_(i=0)^(n-1) f(x_i + h/2)$

== Метод трапеций
$ I approx h ((f(x_0) + f(x_n))/2 + sum_(i=1)^(n-1) f(x_i)) $

== Метод Симпсона
$ I approx h/3 (f(x_0) + 4 sum_(i=1,3,dots)^(n-1) f(x_i) + 2 sum_(i=2,4,dots)^(n-2) f(x_i) + f(x_n)) $

== Правило Рунге для оценки погрешности
$ (|I_n - I_(2n)|) / (2^k - 1) < epsilon $ где $k$ — порядок точности метода.

#grid(
  columns: 2,
  [
#figure(image("image (1).png"), caption: "Блок-схема для метода Симпсона")
],
  [
#figure(image("image (2).png"), caption: "Блок-схема для метода Симпсона")
]

)

= Вычисление заданного интеграла
Интеграл: $ I = integral_1^2 (3x^3 + 5x^2 + 3x - 6) d x $

#figure(
 image("image.png", width: 80%),
 caption: [Подынтегральная функция и область]
)

== Точное вычисление
$ F(x) = 3/4 x^4 + 5/3 x^3 + 3/2 x^2 - 6x $
$ I_"точно" = F(2) - F(1) = 19.dot(3) - (-2.08dot(3)) = 21.41dot(6) $

== Вычисление по формуле Ньютона-Котеса ($n=6$)
Коэффициенты Котеса $H_i$: $1/840 {41, 216, 27, 272, 27, 216, 41}$.
$ I_6 = (b-a) sum H_i f(x_i) = 21.416637 $

== Численные методы ($n=10, h=0.1$)
#align(center)[
  #table(
    columns: (auto, auto, auto, auto),
    inset: 8pt,
    align: center + horizon,
    table.header([Метод], [Значение], [Абс. погр.], [Относ. погр.]),
    [Точное], [21.416667], [0], [0%],
    [Средние прям.], [22.609200], [1.192533], [5.56%],
    [Трапеции], [23.422500], [2.005833], [9.36%],
    [Симпсон], [21.416667], [0.000000], [0.00%],
  )
]


= Листинг программы
Исходный код лабораторной работы доступен в репозитории GitHub:
#link("https://github.com/q-artem/CompMath/tree/main/src/lab3")[github.com/q-artem/CompMath/src/lab3]

= Результаты выполнения программы
Пример работы программы для функции $x^3 - 3x^2 + 5x + 12$:

#show raw: it => align(center, block(fill: rgb("#EEEEEE"), inset: 5pt, radius: 8pt, text(it)))

#raw(
  "------------------------------------------------
Вычисляем интеграл f(x) = x^3 - 3x^2 + 5x + 12 
на интервале [1.000, 2.000]
Метод: Метод Симпсона
Требуемая точность: 0.0001
------------------------------------------------
РЕЗУЛЬТАТ:
Значение интеграла: 13.7500000000
Число разбиений (n): 8
Оценочная погрешность (по Рунге): 0.0000000000
------------------------------------------------
",
  block: true,
)
#pagebreak()

= Выводы
В ходе лабораторной работы были изучены и программно реализованы численные методы интегрирования (прямоугольников, трапеций и Симпсона). В ходе экспериментов была подтверждена теоретическая оценка точности методов и эффективность правила Рунге для автоматического подбора шага. Метод Симпсона продемонстрировал наилучшую сходимость, достигнув точного значения для полинома 3-й степени при минимальном $n$. Метод средних прямоугольников оказался значительно точнее метода трапеций на данном интервале. Дополнительно была реализована логика работы с несобственными интегралами 2-го рода, включающая анализ точек разрыва, проверку их сходимости и адаптивное вычисление значений функции на всей области определения.
